// Arrays
const array = [1, 2];
const [a, b] = array;

// Objetos
const aluno = {
  nome: 'Gabriel',
  sobrenome: 'Tibúrcio',
  linguagemFavorita: 'Javascript'
};

const { nome, linguagemFavorita } = aluno;
